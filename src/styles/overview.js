import styled from 'styled-components';

export default styled.div`
  .confirmed-chart {
    background-color: var(--forgroud-color);
    border-radius: 10px;
    padding: 1em 2em;
    width: 50%;
  }
`;
