import styled from 'styled-components';

export default styled.div`
  display: grid;
  grid-row-gap: 1rem;
  .line-chart {
    position: relative;
    min-width: 50rem;
    /* width: 60%; */
  }

  .pie-chart {
    position: relative;
    /* width: 40%; */
  }
`;
